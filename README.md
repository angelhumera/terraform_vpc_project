# terraform_vpc_project
This project sets the foundation for secure cloud networking. Using Terraform ensures your VPC configuration is version-controlled, reusable, and consistent across environments (dev, test, prod)
